import { PlatformSortPipe } from './platform-sort.pipe';

describe('PlatformSortPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformSortPipe();
    expect(pipe).toBeTruthy();
  });
});
