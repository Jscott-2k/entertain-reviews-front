import { ScoreClassPipe } from './score-class.pipe';

describe('ScoreClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreClassPipe();
    expect(pipe).toBeTruthy();
  });
});
