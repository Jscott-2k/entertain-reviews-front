import { EntertainmentCardDirective } from './entertainment-card.directive';

describe('ReviewCardDirective', () => {
  it('should create an instance', () => {
    // const directive = new ReviewCardDirective(undefined);
    // expect(directive).toBeTruthy();
  });
});
